version: "3.9"
services:
  mongo:
    image: mongo:5.0
    restart: always
    environment:
      - MONGO_INITDB_ROOT_USERNAME=matan
      - MONGO_INITDB_ROOT_PASSWORD=123456
    ports:
      - 27017:27017
    container_name: mongodb